\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  font=\small,
  box/.style={draw=black!65,align=center,text width=43mm,minimum height=13mm,inner sep=3mm},
  flow/.style={-{Latex[length=2mm]},thick},
  check/.style={-{Latex[length=2mm]},dashed,thick}
]
\node[box] (core) {Canonical core $B$\\scalar or vector ensemble};
\node[box,right=15mm of core] (store) {Code chunks and address table\\at state $\sigma$};
\node[box,below=15mm of core] (local) {Strict reconstruction\\and local evaluation};
\node[box,below=15mm of store] (evm) {Registry $r$ and\\EVM evaluation};
\draw[flow] (core) -- (store);
\draw[flow] (core) -- (local);
\draw[flow] (store) -- (evm);
\draw[check] (store.south west) -- (local.north east);
\draw[check] (local) -- node[above,font=\footnotesize] {same $q$} node[below,font=\footnotesize] {compare} (evm);
\end{tikzpicture}
\caption{Publication and verification paths. Solid arrows indicate artifact or
execution flow; dashed arrows indicate reconstruction and comparison.
Verification checks canonical bytes, ordered storage, registry agreement,
and content binding before comparing the integer outputs for the same packed
input $q$.}
\label{fig:architecture}
\end{figure}
